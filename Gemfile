source "https://rubygems.org"

# Matches the GitHub Pages build environment so local previews are accurate.
gem "github-pages", group: :jekyll_plugins

# Needed for `jekyll serve` on Ruby 3+.
gem "webrick", "~> 1.8"
